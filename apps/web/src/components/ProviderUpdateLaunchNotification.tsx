/**
 * Provider update availability is surfaced in the sidebar pill above Settings
 * (`SidebarProviderUpdatePill`). Progress/results while updating also use that
 * pill. The toast-based launch prompts were removed.
 */
export function ProviderUpdateLaunchNotification() {
  return null;
}
