/**
 * Empty-thread surface. The composer is centered by ChatView when there are
 * no messages; we intentionally render nothing here (no logo / heading).
 */
export function EmptyThreadHero() {
  return null;
}
